cpg_sales:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: "cpg_sales"
      schema: "main"
      schema_directory: "{{ env_var('DB_DIR', './data') }}"
      schemas_and_paths:
        main: "{{ env_var('DB_PATH', './data/cpg.db') }}"
