# Paste this into Streamlit Community Cloud → App settings → Secrets
# Replace the values with your actual deployed URLs and keys

API_BASE = "https://cpg-analytics-api.onrender.com"   # your Render service URL
API_KEY  = "cpg-live-key"                              # must match API_KEY in Render env vars
