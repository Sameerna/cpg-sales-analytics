ANTHROPIC_API_KEY=sk-ant-...        # Required only when USE_LLM=true
DB_PATH=./data/cpg.db
MLFLOW_TRACKING_URI=./mlruns
API_KEY=dev-key
# false = fully offline, all charts and Data Intelligence work without an API key
# true  = enables AI Synthesis and Deep Think tabs (requires a valid API key above)
USE_LLM=false
